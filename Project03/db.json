let apiEndpoint = 'https://www.ixl.com/math/grade-4/identify-place-value-names';
async function backEndRestAPI(){
  const response = await fetch(apiEndpoint);
  const data = await response.json;
  //updateDom(data);
}
